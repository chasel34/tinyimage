{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "tinyimage",
  "title": "TinyImage",
  "description": "Compress and convert images locally",
  "icon": "extension-icon.png",
  "author": "lemon_cola",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Media"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "compress-selected-images",
      "title": "Compress Selected Images",
      "description": "Compresses and converts selected images locally using sharp",
      "mode": "view"
    },
    {
      "name": "open-compression-settings",
      "title": "Open Compression Settings",
      "description": "Edit default compression settings for TinyImage",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.6",
    "@raycast/utils": "^1.17.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "vendor-sharp": "node scripts/vendor-sharp-runtime.cjs",
    "build": "npm run vendor-sharp && ray build",
    "dev": "npm run vendor-sharp && ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "postinstall": "npm run vendor-sharp",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
